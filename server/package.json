{
  "name": "matcha-server",
  "version": "1.0.0",
  "description": "Serveur http du projet",
  "main": "index.js",
  "scripts": {
    "build": "npx babel src -d lib/src && npx babel --watch config -d lib/config && npx babel --watch controlers -d lib/controlers && npx --watch babel models -d lib/models",
    "start": "/usr/local/lib/node_modules/node/lib/node_modules/nodemon/bin/nodemon.js --exec npx babel-node src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/gualnet/Matcha.git"
  },
  "keywords": [
    "student.web"
  ],
  "author": "gualnet",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/gualnet/Matcha/issues"
  },
  "homepage": "https://github.com/gualnet/Matcha#readme",
  "dependencies": {
    "express": "^4.16.3",
    "mysql": "^2.16.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.0.0",
    "@babel/core": "^7.0.0",
    "@babel/preset-env": "^7.0.0",
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.3",
    "babel-preset-env": "^1.7.0"
  }
}
